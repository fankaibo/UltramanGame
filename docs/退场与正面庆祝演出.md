# 退场与正面庆祝演出

旧版胜利段落把哥尔赞围绕脚底整体翻转，腹部和尾巴随之陷地；连续网格测量得到最低点为 -0.8547 Unity 单位。英雄则持续背向玩家举拳，胜利字幕叠在画面中央。单张刚进入胜利时的截图不能暴露这些问题，因此本轮先保存了 6.5 秒完整旧版片段。

新版使用失衡、屈膝下沉、稳定落地和淡出的退场顺序。双腿通过骨骼约束留在原落脚处，爪臂随身体下垂，尾链调整角度以保持各节长度并避开地面。落地只触发一次扬尘、小幅镜头震动和原创低沉碰撞声。

英雄先等待怪兽落地，再分两步转向孩子：第一步以左脚支撑，第二步以右脚支撑，移动脚抬起后落回地面；同时举拳庆祝。镜头随后跟到英雄正面，五位英雄均保留头部、拳头和脚底的界面安全空间。体感胜利分数与星章移到左侧，合照提示延后出现在得分下方，让脚部也保持清晰可见。远景随庆祝镜头逐渐扩大覆盖范围，避免左侧露黑；战斗镜头的远景比例保留。

## 时间与流程

- 胜利后 0–0.24 秒：保留失衡起势。
- 0.24–1.10 秒：怪兽屈膝，双脚支撑；1.10 秒触地反馈只播放一次。
- 1.30–2.70 秒：英雄通过两次换脚转向镜头。
- 2.65–3.55 秒：怪兽淡出；随后完整展示英雄正面庆祝。
- 实际自动合照仍须等待胜利至少 6 秒且语音结束；原有取景、额外 3 秒准备时间、5 秒倒数、预览、重拍和再玩不变。

`VictoryMotion` 统一退场、转身和淡出时点，伤害与动作识别规则未改。开发回放的胜利观察期从 3 秒延长到 5 秒，用于截取完整正面庆祝，这不改变用户实际的自动合照等待时间。

## 当前本机证据

`artifacts/victory-staging/comparison.mp4` 左侧为提交 `a14296d` 的旧版，右侧为新版。两侧同样输入和随机种子，195 帧、6.5 秒、无声；各侧源图为 1280×720，拼接视频为 1920×540。30 fps 是离线编码规格，不是游戏帧率测试。

各侧目录包含阶段截图、逐帧 `motion.csv` 和 `render-source.txt`（UTC、Unity 版本及源文件 SHA-256）。`Before` 只保存运行时当前代码，不会回退历史提交；不要在新版代码上重跑并声称得到旧版基准。

`logs/victory-staging-after.log` 与 `after/roster-validation.txt` 已验证：

| 角色 | 英雄最低网格点 | 最大支撑脚位移 | 角色垂直画面范围 |
| --- | ---: | ---: | --- |
| 迪迦 | 0.0000 | 0.0001 | 0.085–0.901 |
| 梦比优斯 | 0.0000 | 0.0029 | 0.100–0.880 |
| 赛罗 | 0.0000 | 0.0028 | 0.102–0.868 |
| 捷德 | 0.0000 | 0.0009 | 0.112–0.869 |
| 格力乔 | 0.0000 | 0.0014 | 0.113–0.891 |

距离为 Unity 场景单位。画面范围以底部为 0、顶部为 1，保留顶部状态栏和底部控制区。所有英雄最终正对镜头，退场只触发一次落地，合照前怪兽隐藏，重置后英雄回到原来的战斗站位。额外将每帧四个视口角投射到远景平面：最大局部坐标绝对值为 0.462，低于检查边界 0.49（贴图实际边界为 0.5），完整覆盖转身镜头。

哥尔赞最低网格点由 -0.8547 改为 -0.0055；剩余误差在尾部、约 0.0055 场景单位，低于本项目 0.035 的地面容差。这里描述的是模型几何检查，不能把它当作物理碰撞模拟。

- `logs/victory-staging-check.log`：33 项 Python、300 项 C#、含跨进程通信后 303 项检查通过。
- `logs/victory-staging-rigged.log`：连续攻防、左右爪、大招近景、阴影及胜利通过，原有爪臂接触与前冲保持。
- `logs/victory-staging-release-build.log`：macOS 构建及本地开发签名通过。
- `artifacts/cinematic-combat/player/20260926T201736365338Z/validation.json`：实际播放器在 71.43 秒完成整局（32 拳、2 次光线、1 次防御、1 次受击、暂停恢复、胜利），保存 24 张本次截图，包含退场、转身、完整正面庆祝。落地时点与原创落地声均只出现一次；程序集 SHA-256 保留在验证文件中。此检查使用开发输入，不使用真人摄像头。

- `artifacts/victory-staging-release-guided/guided-validation.json`：最终构建以合成摄像头姿势在 138.9 秒完成胜利、自动合照、预览、重拍、短暂断流恢复及手势再开局；键鼠事件为 0，真人图片为 0，保存两张 TEST 合照。该目录的 `native/victory-hero.png` 展示最终体感界面，合照提示位于得分下方，角色脚部不被遮挡。

## 复现

```sh
/Applications/Unity/Hub/Editor/6000.6.0f1/Unity.app/Contents/MacOS/Unity \
  -batchmode -projectPath "$PWD/unity" \
  -executeMethod UltramanGame.Editor.VictoryReview.After \
  -quit -logFile "$PWD/logs/victory-staging-after.log"
```

这轮完善胜利段落的连续动作和构图，不代表已达到参考机台的整体动作丰富度或影视级模型品质。帧率专项仍按用户要求后置。
