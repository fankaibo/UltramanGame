# 格挡受力演出（2026-09-20）

这一轮针对参考录像中攻防双方都会回应接触的表现，补上成功格挡后的连续动作。没有改变姿态识别、护盾判定时间、伤害、血量、蓄能和语音反应时间，帧率专项优化仍后置。

## 修改

- 成功格挡时，英雄上身短暂后仰承力，头部略作反向调整，随后回到防御姿势；下肢保持原动画，不整体倾倒或滑移角色。迪迦与另外四位英雄分别映射各自的骨骼名称。
- 怪兽被挡住后胸部与头部后仰、攻击爪随躯干收回，接着沿原攻击片段恢复。只有实际的格挡事件触发，不因一直举着护盾反复播放。
- 护盾中央更透明，改为较疏的六边形纹理；撞击时短暂压缩，光波从爪击投影点扩散。成功格挡时移除额外悬空的大圆环，保留命中火花与原护盾音效。
- 新增五英雄的格挡渲染复核及实际播放器 `guard-impact.png` 取证；暂停和新一局清除旧反应。
- 迪迦左右拳加入不改变手部片段的小幅转肩、胸部下沉和头部延迟跟随，左右方向相反，补足出拳的重量转移。
- 无键鼠验证在能量满后模拟收手，再保持大招姿势；这样检查的是玩家能听懂并完成的流程，而不是把同一姿势持续到自动触发。

## 当前证据及限制

最终验证目录为 `artifacts/guard-impact-20260920T100941Z/`：五位英雄均测得上身偏转约 12°、怪兽反弹约 13.77°，脚部位移为 0；恢复、暂停和新局清理均通过。对应图片在 `roster/`，源码哈希在 `roster/render-source.txt`。

同一构建的 16 秒实际攻防序列通过：左右拳各 2 次、格挡 1 次、受击 1 次、左右爪交替、拖尾 4 个可见像素检查、重置清理通过，记录为 `exchange/after/validation.txt`。完整检查为 33 项 Python 与 287 项 C# / 通信检查，`checks.log` 中为绿色结果。macOS 构建及本地签名通过，构建日志为 `build.log`。

实际播放器在 68.98 秒完成 32 次普攻、2 次大招、格挡、受击、暂停恢复和胜利；本次程序集 SHA-256、截图和日志在 `artifacts/cinematic-combat/player/20260920T101057234509Z/validation.json`。无键鼠引导在本目录完成自动合照、断流恢复、重拍、再玩和第二局，键鼠事件为 0；记录为 `guided/guided-validation.json`。

这些证据证明本轮改动已经进入可试玩构建，但不代表角色网格、连续动作数量和镜头语言已经达到参考商场街机的最终美术品质。

## 继续验证

在本机终端运行以下命令。脚本仅使用合成动作和 TEST 合照，不打开真实摄像头；任一步失败会停止，不会自行提交或推送。

```bash
bash /Users/fankaibo/Documents/ChatGPT/Game/scripts/verify_guard_impact.sh
```

它依次执行五英雄格挡复核、同输入 16 秒攻防序列、现有全套检查、macOS 构建、实际播放器整局和无键鼠合照/重拍/下一局验证。每次使用独立 UTC 输出目录，避免旧结果被误当作本次成功。最新构建已完成这些步骤；后续若改动相关演出，应重新生成一套新的 UTC 目录。

上一版已提交并推送的可试玩版本为 `db03730`；本轮变更验证后再提交并推送。角色素材细节和整体动作丰富度仍未达到参考街机效果，不能以本轮通过测试代替最终美术验收。
