# 本次画面和声音资源

2026-09-08。城市背景使用内置 imagegen 工具生成，已复制到项目；音乐与音效由本项目脚本合成，不依赖外部音乐服务。摄像头图像没有用于生成素材。

## 城市背景

文件：[CityDusk.png](../unity/Assets/Resources/Art/CityDusk.png)。用于舞台后方的静态城市背景，前景角色、光圈、粒子和光线由 Unity 实时渲染。画面为 2D 背景与 3D 舞台结合，尚未制作可自由移动镜头的完整城市关卡。

工具：内置 `image_gen.imagegen`，未使用 CLI/API fallback。最终生成提示词如下：

```text
Use case: stylized-concept. Asset type: wide background matte painting for a Unity family superhero motion-control game. Generate a polished cinematic 16:9 futuristic Japanese coastal city at blue hour, seen from a slightly elevated training plaza. Distant luminous modern skyline, one elegant needle tower, layered cobalt and indigo buildings with cyan window lights, a soft violet nebula arc in a deep navy sky, a calm warm golden glow low on the horizon. Child-friendly adventurous heroic mood, clean stylized 3D animation concept art, beautiful atmospheric perspective, rich but controlled teal, blue, lavender and amber palette. Composition: skyline primarily in middle third, open uncluttered lower half in dark blue for real 3D characters and circular arena rendered by the game on top, quieter upper left for UI title, entire background composition wide and spacious. No people, no heroes, no monsters, no close foreground objects, no lettering, no logos, no UI, no watermark. This is a final in-game background art asset, not a screenshot or mockup. Landscape 1792x1024 or comparable wide image.
```

角色现为以商品参考图重新制作的迪迦与哥尔赞动作插画，每名角色 8 个姿势，叠加呼吸、出拳位移、受击和退场。当前版本优先外形相似度，不再逐关节跟随；详细来源与最终提示词见 [角色造型参考](角色造型参考.md)。

## 音乐与音效

源脚本：[generate_audio.py](../scripts/generate_audio.py)，输出到 [Resources/Audio](../unity/Assets/Resources/Audio)。这些 WAV 和 Unity 导入配置随仓库保存；运行脚本可确定性重建。使用固定随机种子，不下载影视配乐。

- 准备音乐：90 BPM，8 小节，21.33 秒循环。
- 战斗音乐：108 BPM，16 小节，35.56 秒循环。
- 音效：挥拳气流、命中、护盾、光线、变身、恢复、胜利，共 7 种。
- 格式：32 kHz、16-bit PCM；音乐与胜利为立体声。
- 音色：轻电子合成风格，和弦、拨弦、低音及节奏分层。不是影视原声或真人管弦乐录音。

音乐随准备/战斗状态交叉淡入淡出。语音播放时降低音乐音量；暂停时压低音乐并停止尚未播出的战斗台词。警告和胜利语音优先，普通台词仅短暂排队，过期或场景变化后取消。

`F4` 打开声音设置，可调整总音量、背景音乐音量、开关音乐及静音；设置保存在本机。`F11` 切换全屏。画面与设置中没有网络音频播放。

## 中文引导

[generate_voice.py](../scripts/generate_voice.py) 使用本机 macOS Tingting 声音生成 12 条固定引导，包括欢迎、变身、战斗、防御预警、格挡、恢复、能量、光线、胜利、恢复战斗、挥拳教学和光线教学。

系统语音不是角色演员配音。生成文件不纳入 Git；macOS 构建脚本自动生成，且检查音频内容非空。Unity 构建入口进一步检查 21 个必需音频资源能成功加载并具有有效时长，避免空文件随构建通过。

## 自选背景音乐

用户优先选择毛华锋《奇迹再现》。开发过程中，下载文件夹出现了用户准备的原唱 MP3；现已在本机成功导入，播放器报告约 236.8 秒、双声道，整局战斗中持续播放。文件保留在用户原位置，不随源码或游戏包分发，也不将内置配乐标为《奇迹再现》。官方歌曲页面可见 [Spotify 原唱曲目](https://open.spotify.com/track/3zAieIyKyNODQn6y2sxbFq)。这只是来源说明，游戏不抓取或嵌入音乐平台音频。

按 F6 或在 F4 设置中选择“导入音乐”，使用 macOS 文件选择器加载本机 MP3、WAV、OGG、AIFF。支持 1 秒至 15 分钟、小于 80 MB 的普通音频。路径仅保存在本机 PlayerPrefs，文件不上传、不拷贝进项目；下次打开会自动加载。如果文件移动、格式不支持或损坏，会显示提示并保留当前配乐。“恢复内置配乐”清除选择。

导入歌曲在准备、变身、战斗、胜利中连续循环，语音时自动降低音乐音量，静音和音量设置同样生效。原生组件源码为 `native/MusicPicker.mm`，构建脚本自动编译，生成的二进制不纳入 Git。
