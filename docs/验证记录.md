# M0 验证记录

日期：2026-09-08。设备：MacBook Air / Apple M3 / 16 GB。编辑器：Unity 6000.6.0f1（f7f8ed4d1e24）。区分规则检查、真实模型测试、实际运行和真人体验。

## 自动检查

| 验证项 | 已通过的范围 |
| --- | --- |
| Python 协议与服务 | 12 项 unittest：姿态及预览消息、来源、NaN、序列、最新帧、多个订阅者、遮挡状态、端口释放、最新帧采集和相机失败释放 |
| 实际 C# 核心文件 | .NET 8 编译与 56 项检查：招式、暂停恢复、无失败和胜利；包括 14 项人体存在回归及 8 项预览检查、6 项宽松儿童动作与连续挥拳检查 |
| Python → C# 通信 | 3 项额外集成检查（姿态 2 项、预览 1 项）；使用系统分配的独立端口，可与游戏同时运行 |
| 真实模型 | MediaPipe 1.0.1、Lite 模型、Metal GPU / SRGBA；5 张生成黑色画面推理完成，共 55.7 ms，结果为 0 个人体 |
| 模型完整性 | 5,777,746 字节，SHA-256 与 models/manifest.json 固定值一致 |

合计 **71 项不同自动检查**。集成程序重复执行的 56 项核心检查不另计。黑色画面推理不能证明真人识别准确率或电视延迟。复现命令为 `./scripts/check.sh`、`MPLCONFIGDIR="$PWD/.cache/matplotlib" .venv/bin/python scripts/smoke_model.py` 和 `./scripts/build_macos.sh`。[GitHub Actions](https://github.com/fankaibo/UltramanGame/actions) 只执行无相机检查。

## 已执行的本机验证

| 验证项 | 观察结果 |
| --- | --- |
| Unity 导入、编译与构建 | 已完成，包括误暂停修复与右下角预览的重新构建；本机产物为 unity/Builds/TigaTraining.app |
| 实际画面 | 中文、角色、血量和能量可见；已观察键盘变身、挥拳、暂停提示和合成动作胜利画面 |
| 整局 Python → Unity | 画面明确标记“合成动作测试”；完成变身、预警、防御、受击恢复、蓄能、光线与胜利。该轮 13 次挥拳、1 次成功防御，怪兽力量最终为 0 |
| 前置摄像头短测 | 授权后 5.04 秒取得 95 帧，95 帧返回人体；整体处理 18.9 FPS，平均推理 18.5 ms，包含首次启动影响 |
| 真人到角色 | 用户确认人物能跟随动作；真实相机输入触发过变身。同时发现误暂停问题，修复见下文 |
| 10 秒链路诊断 | 10.02 秒取得 231 帧，全部有人体；193 帧全臂关节达标。35 帧左肘、5 帧右腕不可靠，部分重叠，共 38 帧未全臂达标；平均采集后到本机接收 11.4 ms |
| 两项修复后的对照 | 最新 10.01 秒取得 233 帧，233 帧均符合人体存在条件，同期 Unity 追踪状态变化为 0；这是短时稳定性结果，不等于完整儿童动作验收 |
| Unity 内预览 | 合成图像在右下角正常解码显示（320×240）；实测 F3 收起/显示、F2 键盘模式隐藏与切回恢复；停止测试图像服务后旧图清空并出现断流提示 |
| 真实预览与性能 | 上一版 8 秒采样：姿态 187 帧、23.3 FPS，平均接收帧龄 10.4 ms；预览 65 帧、8.0 FPS，平均帧龄 12.8 ms，最大 JPEG 12,566 字节。Unity 日志确认 camera 来源的 320×240 图像已解码显示，不保存真人截图 |
| 本机端口冲突 | 发现其他应用占用 8765、8766，启动器改用系统自动分配端口；合成与真实模式均成功连接，未操作其他应用 |
| 退出生命周期 | 启动入口管理的游戏关闭后，启动器退出；合成试玩后核对 8765 端口已释放，真人试玩关闭后也确认启动器退出 |
| 中文引导 | 本机 Tingting 生成 12 条 AIFF，已验证非空、Unity 导入和播放器实际播放状态；实际扬声器音量和电视听感待现场确认 |

接收帧龄不等于动作到电视显示的延迟。相机不录制或保存图像；本地运行日志及质量汇总不纳入 Git。

## 动作与视听体验迭代

本次修改覆盖三类反馈：招式不易触发、人物像木偶、画面反馈不够流畅。

- 采集与推理分开，仅保留最新帧；预览目标提高至约 15 FPS。
- 放宽略弯的挥拳、较短的 L 形光线姿势与胸前防御；仍保留收手后再出拳、不因遮挡触发和断流暂停的约束。新增 6 项规则回归通过。
- 角色采用固定长度的关节运动，补上髋部、胸部、头部、呼吸、转身、迈步和收拳。完整精细角色模型仍未完成。
- 接入生成的城市夜景、训练台、发光环、命中粒子、光线、胜利卡片和动画动作图示。实际窗口检查过界面、设置和合成战斗胜利。
- 合成战斗这一轮完成 11 次命中、2 次格挡和 2 次光线，最终怪兽力量为 0。
- 本机摄像头与新版游戏一起运行的 10 秒采样：动作流 233 帧，23.2 FPS，233 帧返回人体，平均接收帧龄 8.1 ms、最大 25.4 ms；预览 152 帧，15.2 FPS，平均帧龄 8.9 ms。
- 渲染短测：合成模式 59.4 FPS，真人模式 58.9 FPS。真人模式首轮采样出现过 98.1 ms 的单帧耗时，因此不能称作完全没有卡顿；随后将音乐、音效与语音改为启动时预加载，避免第一次出招时读取音频资源。最终准备阶段追加一轮 10 秒复测为 59.6 FPS、最慢帧 21.1 ms；场景不同，不据此声称所有卡顿已消除。
- 音乐 WAV 已检查非空、峰值和循环接缝。2 首循环音乐、7 种音效与 12 条语音通过构建资源检查。播放器确认 welcome/transform/battle 等语音 `playing=True`；语音期间音乐音量约 0.078，结束后约 0.218（本轮默认设置）。
- 受限执行环境曾让 `say` 返回成功但生成空 AIFF；改为调用可访问本机语音服务的环境，并加入生成后的非空检查、临时文件替换和 Unity 构建前有效时长检查。修复后的欢迎语为 3.61 秒，播放器实际加载成功。

以上数据不是儿童动作准确率，也不是摄像头曝光到电视发光的端到端测量。新版真实手感、音量与音乐偏好仍需结合现场试玩反馈继续调整。

## 真人误暂停修复

旧代码将任一肘/腕低置信度视为整个人体丢失并立即暂停。用户在镜头前动作时反复收到恢复提示；采样中的 38 帧手臂不可靠与此吻合。

另一次 8 秒对照取得 185 帧，其中 151 帧的两肩归一化距离不超过旧阈值 0.08，同期游戏发生 6 次追踪状态切换。因此还移除了人物存在判定中的肩距下限，避免侧身或较远站位被误判为离开。

新代码按双肩可见性判断人物存在，每只拳使用本侧可靠手臂；举手变身不要求肘部可靠，防御和光线仍需双臂看清。距最后可信上半身采集时间允许 600 ms 缓冲，旧帧不能延长缓冲。遮挡不触发招式，不保留过时防御，也不会让保持的光线再次释放。真正离开或断流仍会暂停。

修复已有 14 项回归检查，并已重新构建、打开真实相机模式。修复后的真人连续体验仍需结合用户反馈确认，不能以模拟检查代替。

## 兼容问题与边界

MediaPipe 默认 CPU 路径在本机崩溃，改用显式 Metal GPU / SRGBA 后通过模型和相机测试。Unity 6000.6 未能加载 PingFang 系统字体，改为内置 Noto Sans SC 后实际窗口文字正常。相机首次权限问题已由用户授权解决。重启时曾出现首帧等待过短，改为首次取景最多等待 5 秒、运行中连续失帧最多等待 2 秒；重试 5.04 秒取得 117 帧，其中 94 帧返回人体，平均推理 8.1 ms，并已重新接入游戏。集成测试使用独立端口，避免与游戏争用 8765。

仍待完成：修复后真人连续试玩、招式各 10 次的识别记录、儿童比例调参、小米电视接线与延迟测试、更多连续动画和完整的变身演出。当前已按用户选择改为动作插画，旧三维模型不再使用。

## Bing 参考角色重建（2026-09-08）

- `./scripts/check.sh` 再次通过：12 个 Python 测试、56 个 C# 检查、3 个额外跨进程检查，共 71 项独立检查。角色更新没有修改动作判定和战斗核心规则。
- Unity 6000.6.0f1 已重新编译、构建 macOS Player；`CharacterReview.Render` 使用游戏里的实际网格和材质渲染正面、65 度侧面和背面。修正了首轮检查中发现的关节空洞、尾巴分段断裂和尾根间隙。
- 最终两名角色合计 190,640 个顶点、257,516 个三角面、79 个渲染对象；通过遍历全部顶点的非有限数检查。此统计包含材质合并后保留的顶点，并非去重后的模型顶点数。
- 新角色通过 Python 合成动作 → 原生 Unity 完成一整局：12 次挥拳命中、1 次防御、2 次光线、怪兽生命归零、进入胜利；实际窗口已核对双手举起的胜利姿态。证据位于本机忽略目录 `logs/character-battle-proof.log`。
- 合成动作完整战斗前的 10 秒采样为 59.6 FPS、最慢帧 22.8 ms。第一次切换全屏的采样出现约 1.54 秒停顿，不能用它代表稳定帧率；真实摄像头首次启动采样也曾出现约 1.45 秒停顿。因此开发版现在保留连续两个 10 秒采样窗口，便于区分启动阶段和随后运行。以上不代表小米电视投屏时的帧率或端到端延迟。
- `F5` 角色展示已在原生窗口打开；每次按左右方向键转动 30 度，F5 / Esc 返回游戏。展示使用单独的展示状态，进入时暂停战斗，真实相机画面不显示在展示页。
- 最终构建使用真实前置摄像头复测，日志确认 `source=camera size=320x240`；连续两个 10 秒窗口均为 **59.6 FPS**，最慢帧分别为 **23.6 ms / 35.2 ms**。这是本机窗口模式的渲染测量，不是动作识别准确率测试。
- 最终原生窗口已验证 `F5` 进入角色展示，单次 `Right` 后两名角色发生可见的 30 度转动。新版保持在角色展示页，按 F5 返回体感游戏。


## 动作插画与本机歌曲导入（2026-09-08，当前版本）

本节取代前文关于当前角色仍用三维网格和 F5 旋转的描述；旧数据保留为开发历史。

- 新版使用迪迦、哥尔赞各 8 张动作插画，两个角色绘制对象。图集均为 1448×1086、4×2 排列；逐帧边缘检查确认 16 个姿势没有跨越切片边界。首轮棋盘背景和跨格的手部问题通过重新生成素材修正，绿色底色由实际游戏 shader 消除。
- Unity 6000.6.0f1 已编译并构建 macOS Player。`CharacterReview.Render` 用游戏 shader 输出 8 组共 16 个姿势；检查过准备、光线、变身与受击画面，角色脚部以实际素材最低点对齐。
- 原生游戏已核对新角色、胜利画面、F5 展示和 Right 切换动作；展示页的操作从旋转改为切换姿势。摄像头输入负责招式事件，取消逐关节身体跟随。
- `./scripts/check.sh` 通过 12 项 Python、56 项 C# 和 3 项额外跨进程验证，仍为 71 项独立检查。动作识别与战斗核心未修改。
- 自动合成输入通过 Python → Unity 完成变身、11 次挥拳、2 次格挡、2 次光线和胜利。过程中本机《奇迹再现》MP3 成功载入（播放器约 236.80 秒、双声道、loop=true），音乐在准备、战斗和胜利阶段保持播放。证据：本机忽略目录 `logs/animated-battle-music-proof.log`。
- 语音播放时音乐音量约 0.078，普通战斗时约 0.218；胜利语音时约 0.039。这里只验证播放器状态与音量，不代表电视扬声器听感。
- 此次自动战斗的两个 10 秒采样分别为 58.8 / 57.1 FPS，最慢帧 78.0 / 373.5 ms。存在卡顿样本，不能声称所有动作已完全流畅，也不是电视端到端延迟数据。
- 本机文件选择器采用附着于游戏窗口的异步 NSOpenPanel，避免阻塞 Unity 主循环；原生二进制同时支持 arm64、x86_64，由源码构建。修复了包含加号的中文文件名 URI 编码。实测 F6 → 选择歌曲 → Return 后出现 `[MusicPicker] selected` 和 `[LocalMusic] loaded`，F6 → Esc 取消后保持已有歌曲。
- 它只记住用户选择的本机路径；音频不复制到项目、不上传 GitHub。异常文件保留当前音乐并显示提示，文件移动后需要重新选择。
- 最终构建重启到真实摄像头模式，未传入 `--music`：日志确认自动重新加载已保存的歌曲、`source=camera size=320x240`，连续两段 10 秒采样为 **59.3 / 59.2 FPS**，最慢帧 **21.1 / 55.7 ms**。重启记忆已验证；不保存真人画面。
- 后续长时间运行出现一次 MediaPipe 原生进程退出：`Error creating pixel buffer: -6662`（GPU 像素缓冲区创建失败）。游戏按断流规则暂停，短时帧率数据不能证明长时稳定；已重启恢复。此次改动未修改原生推理库，该长时问题仍待定位。

## 背侧 45° 战斗视角（2026-09-08）

- 迪迦替换为 `TigaRear45Actions.png`，1448×1086、4×2 背侧动作图集。8 个切片逐一检查，均有完整角色像素且没有角色像素接触格子边界；哥尔赞继续使用已有正面图集。
- 两人站位的 X、Z 差值均为 3.3，形成 45° 对角线。Unity 实际透视渲染覆盖 16 个姿势与出拳、护盾、光线 3 个战斗画面；前冲沿两人连线的断言通过。修正了光线起点偏低和远处地台光环穿过怪兽身体的问题。
- `./scripts/check.sh` 的 71 项独立规则及跨进程检查通过。此次没有修改姿态识别或战斗核心规则。
- Unity 6000.6.0f1 编译、macOS Player 构建成功；实际窗口检查过准备、变身、F5 展示、Right 切换收拳与出拳、胜利。提示字幕移至怪兽下方，胜利卡片收窄；蓄力和命中提示移至左侧，避免顶部横条遮住怪兽正面。
- Python 合成动作 → 原生 Unity 完成变身、12 次挥拳、2 次格挡、2 次光线、怪兽力量归零并进入胜利；取景窗口明确标注“合成测试”。日志保存于本机忽略目录 `logs/rear45-battle-proof.log`，没有保存真人画面。
- 已保存的本机《奇迹再现》自动载入，播放器报告 236.80 秒、双声道、循环播放。两段 10 秒渲染采样分别为 57.9 / 58.9 FPS，最慢帧 146.5 / 31.2 ms；此数据不能代表电视延迟，也不能证明长时间摄像头稳定性。前节记录的 MediaPipe 长时原生错误没有在本次视角改动中修复。

## 本地连续前推动作与宽松必杀技（2026-09-08）

用户进一步确认“向前出拳不易触发，举手或压手反而容易触发”。代码检查发现旧判断只使用二维肩腕距离和肘角，忽略了协议中已有的 `z`。此次保留本机 MediaPipe Lite，在 Unity 中增加短时间窗口的相对前后运动判断；没有新增云端调用，也没有声称已训练动作分类模型。

- 新增 `PunchMotion`，区分前推、侧前移动、纯上下移动和身体整体移动；前推不要求肘部可见，仍要求本侧肩部与手腕可靠。保持伸手不连发，不将深度上伸出的单拳当作持续护盾。
- 光线支持宽松抬手组合和双手前推，增加能量/阶段限制、短暂抖动容忍及明确放松后重置。针对“能量满时，保持普通前推拳被误当光线”的交叉案例，先复现失败，再修复并复测通过。
- 新增 27 项核心检查，覆盖 15/30/60 FPS、前推回收再出拳、左右手、肘部遮挡、较小人体、举手压手、单帧深度跳变、小幅噪声、身体平移、腕部遮挡后恢复、换流、重复帧、宽松 L 形、双手前推、短暂遮挡、放松重置、能量门槛及招式冲突。完整检查为 12 项 Python + 83 项 C# + 3 项额外跨进程验证，共 **98 项独立检查**；日志位于本机忽略目录 `logs/motion-all-checks.log`。
- Python 合成输入轮换侧前/L 形与前推/双手前推；前推序列故意让手肘不可靠、手腕横向位置几乎不变。原生 Unity 日志在胜利前记录了 8 个“向前挥拳”事件；整局 12 次挥拳命中、2 次格挡、2 次光线并胜利。事件数量不等于命中数量，也不是真人准确率。证据：`logs/local-motion-battle-proof.log`。
- 此次合成战斗两个 10 秒窗口为 59.4 / 59.2 FPS，最慢帧 24.0 / 35.6 ms；《奇迹再现》仍从本机路径载入并循环播放。固定语音更新为两种必杀技提示，取景小窗增加短暂动作反馈。
- Unity 6000.6.0f1 编译与 macOS 构建通过。最终补充的“伸拳保持不算防御”规则经过完整检查后重新构建。以上验证没有测量孩子真人识别准确率；单摄像头估计深度仍可能抖动，须根据新版现场试玩反馈继续判断。此前 MediaPipe 长时像素缓冲区失败问题没有在此修改中修复。

## 移除深色训练圆盘（2026-09-08）

按用户要求删除三维圆盘、外围双圈及 12 个装饰灯块、两名角色常驻的脚下标记。城市背景原图未修改，按原比例放大 1.5 倍并以底边对齐，使背景广场位于两名角色脚下，避免去掉圆盘后怪兽悬在城市上空。相机视野先更新再计算背景尺寸，保证切换展示视角时仍完整覆盖画面。

Unity 的实际场景渲染检查通过，覆盖 16 个动作姿势及出拳、护盾、光线 3 个战斗画面；前冲方向断言仍通过。检查了去除圆盘后的站位与背景衔接。此变更未改动动作识别和战斗规则，没有为装饰删除新增规则测试。

Unity 6000.6.0f1 的 macOS 构建成功，已重新打开真人模式；启动日志确认摄像头预览和本地音乐正常载入。

## 动作示意与可调血量（2026-09-08）

修复 HUD 动作示意的线段矩阵：先在 HUD 局部坐标中旋转和平移，再叠加屏幕缩放。原生窗口和全屏均已实际查看，举手、挥拳、护盾及光线的小人连线完整，不再只剩关节点。设置打开时仅提交弹窗控件，避免背景按钮参与弹窗输入。

怪兽默认血量改为 50 次普攻，在游戏设置的战斗页可调 10–200 次，步长 10。第 15 次普通攻击命中蓄满光线，防御不计能量，光线伤害仍为 9。补充 16 项行为检查，覆盖默认及自定义血量的精确胜利边界、第 14/15 拳、蓄能封顶与提示、光线消耗和再次蓄能、防御及未命中不增加能量。完整检查通过：12 项 Python + 99 项 C# + 3 项额外跨进程验证，共 114 项独立检查（跨进程运行输出 102 含重复的 99 项）。日志：`logs/action-settings-checks.log`。

Unity 编译、macOS 构建及 16 个姿势、3 个战斗渲染画面通过。原生界面使用方向键调到 60，回车应用，新一局显示 60/60；退出并重新启动后仍为 60/60，随后恢复 50/50。自动点击未产生可观察的按钮鼠标事件，因此未将鼠标按钮和拖动计为验证通过；设置支持左右键和 Enter，已验证可用。开发构建保留 `[UI]` 点击诊断，便于后续现场反馈定位。

## 战斗主体放大与文字缩小（2026-09-08）

按用户要求，变身完成后镜头从 35° 平滑拉近至 27°，人物在同一视口的投影尺寸约增加 31%；光线时为 26°。战斗标题、能量、预警、字幕、胜利及暂停提示均使用更小字号和面板，动作卡片下移并缩小，摄像头预览保留在右下角。准备变身仍使用完整动作引导。

Unity 编译和 macOS 构建通过；原有 16 个角色姿势与出拳、护盾、光线三个战斗渲染画面通过，前冲方向断言仍通过。渲染检查改为观察镜头过渡完成后的最终取景。已在原生窗口与全屏的合成动作模式检查紧凑 HUD，角色头部和脚部完整可见，动作连线、血条、15 格能量与缩小的取景窗口显示正常。此变更仅调整呈现，没有新增战斗规则测试。

合成动作整局完成 32 次普攻命中、2 次光线、6 次防御并胜利；已查看缩小后的胜利面板，以及按 R 重开后恢复的完整变身引导。此轮两个渲染采样窗口为 59.4 / 59.1 FPS，最慢帧 21.4 / 38.8 ms。日志保留于本机忽略目录 `logs/battle-view-play-proof.log`；合成运行不代表真人动作准确率。


## 怪兽前冲、挥爪与接触反馈（2026-09-08）

用户选择参考网上片段制作场内动画、保持战斗连续。参考来源与观看位置记在《角色造型参考》；游戏没有插播或打包原作录像。怪兽预警后进入独立攻击阶段，向迪迦前进 2.25 个场景单位，在第 0.4 秒才结算一次接触，随后退回；总攻击时长 1.05 秒。新增蓄力光圈、冲刺拖尾、爪痕、蓝色格挡/橙色受击反馈及前冲音效。

修正原先预警结束即受击、画面随后才轻微前移的不同步问题。补充 19 项行为检查，覆盖 15/30/60 FPS 的接触时机与单次结算、途中举盾、接触前后松盾、暂停恢复、光线取消攻击、最后一拳提前获胜。完整检查通过：12 项 Python + 118 项 C# + 3 项额外跨进程验证，共 **133 项独立检查**；跨进程执行输出的 121 含重复的 118 项，不重复计数。日志：`logs/monster-attack-checks.log`。

Unity 实际场景渲染通过，覆盖 16 个姿势、3 个玩家攻击画面和 8 个怪兽攻击阶段画面。接触检查同时断言怪兽前进至少 2.2 个单位、仅一次受击和爪痕可见，归位/暂停后爪痕关闭。查看接近、受击和格挡画面后调整了爪痕起点，使其跟随怪兽接近，角色头脚仍在放大的战斗视野内。日志：`logs/monster-attack-render.log`。

macOS Player 构建成功。合成动作 → 原生 Unity 完成 32 次普攻命中、2 次光线、4 次格挡并胜利；已查看全屏蓄力预警、紧凑 HUD 和胜利面板，日志中每轮前冲后正常发出受击或格挡事件。两个 10 秒渲染窗口为 59.4 / 59.1 FPS，最慢帧 21.5 / 48.5 ms；本机歌曲继续载入循环播放。证据：`logs/monster-attack-play-proof.log`。这些数据是本机合成输入验证，不代表孩子动作准确率或电视投屏延迟。

最终补充普通攻击命中粒子跟随前冲怪兽的位置后，Unity 再次编译并构建成功。已重新打开真实摄像头模式，启动日志确认 `source=camera size=320x240` 和本机歌曲 `loop=true`。没有保存真人取景画面；此前记录的 MediaPipe 长时像素缓冲问题不在此次动画改动的修复范围内。


## 迪迦大招战吼与招式字幕（2026-09-08）

用户先选择日语原声，再确认“只要迪迦本人原声，可以用战吼配招式字幕”，并试听确认采用 Voicemod Tuna 社区上传的短录音。音频来源不冒称官方发布，素材资料详见《美术与音频说明》。

将该录音转换为 0.888 秒、48 kHz、单声道、16-bit PCM AIFF，保存在本机忽略的 `Resources/Voice/beam_original.aiff`，不由 `generate_voice.py` 覆盖。接入实际 `GameCue.Beam`，同时显示 2 秒招式名称，致胜后的短暂字幕也保留。战吼播完后才允许胜利语音和庆祝音效播放；暂停、静音和重开照常立即清理。

Unity 6000.6.0f1 编译与 macOS 构建成功，构建检查验证了可选原声资源可读取且不超过 3 秒。现有 133 项独立规则与回环通信检查仍全部通过（12 项 Python、118 项 C#、3 项额外跨进程验证），日志为 `logs/beam-voice-checks.log`。此次未改动战斗伤害和姿态识别，也未为音频呈现增加重复的核心规则测试。

原生 Player 合成试玩把怪兽临时设为 20 次普攻，以覆盖光线直接致胜的情况：完成 15 次普攻、2 次格挡、1 次光线后胜利。日志按顺序记录 `cue=Beam`、`key=beam_original playing=True length=0.89`、`cue=Victory`、`deferred=victory until=beam-finished`，最后才是 `key=victory playing=True`，确认实际播放未被胜利提示抢断。已查看胜利结果，并通过设置恢复、应用 50 次普攻血量。证据：`logs/beam-voice-play-proof.log`。

最终已重新启动真实摄像头版本；启动日志确认相机预览、已保存的本机歌曲以及 `beamOriginal=True`。原声试听由用户确认，播放器和播放顺序由上述原生运行验证。

## 热血青年英雄中文配音（2026-09-08）

用户试听两段实际生成的样音后选择 A「热血青年英雄」，用于中文引导和战斗提示。以该新设计的合成样音统一生成 12 条台词，没有使用用户或演员录音复制音色；迪迦战吼仍是此前确认的本机短录音。公开生成服务达到免费额度后，使用本机 MLX 模型完成全套台词；模型文件从 ModelScope 获取并逐项校验，主要权重与 Hugging Face 记录的 SHA-256 一致。该模型仅用于制作，游戏和常规构建均不需要加载它。

成品为 24 kHz、单声道、16-bit PCM WAV，12 条合计 28.93 秒。修整首尾空白、统一音量并保留峰值余量；防御预警 2.24 秒，小于怪兽 2.4 秒蓄力阶段。使用独立的本地 Qwen3-ASR 0.6B 模型转写成品，忽略标点后 12/12 条文字与台词逐字一致。音色偏好由用户对 A 样音的试听确认；转写核对不等同于人工听审。日志：`logs/anime-voice-pack-validation.log`、`logs/anime-voice-transcripts.log`。

语音包及台词/来源/哈希清单随源码保存。`generate_voice.py` 默认离线安装该包，先校验完整台词和每条文件的哈希与格式，再统一转换；全部转换有效后才替换 12 条引导。显式 `--voice Tingting` 保留系统声音备用。原声文件 `beam_original.aiff` 的 SHA-256 在安装前后均为 `20387416bec22d85733cda9f373125a9821a6f9680dcea3b7698541957097e33`。

完整 `scripts/build_macos.sh` 通过，证明常规构建默认使用英雄配音且 Unity 能导入全部资源。现有 133 项独立规则和回环通信检查通过（12 项 Python、118 项 C#、3 项额外跨进程验证）。此变更未改变姿态识别、伤害或蓄能规则，也未新增重复的核心规则测试。日志：`logs/anime-voice-build-command.log`、`logs/unity-build.log`、`logs/anime-voice-checks.log`。

原生 Player 合成动作整局完成 32 次普攻、4 次格挡、2 次光线并胜利，已查看胜利画面。播放日志记录新版欢迎 3.07 秒、变身 1.61 秒、防御提示 2.24 秒、能量提示 3.98 秒、胜利 2.86 秒；两次大招均选择独立的 `beam_original` 0.89 秒录音。此次普通攻击完成最后一击，没有重复测试此前已验证的光线致胜抢断场景。证据：`logs/anime-voice-play-proof.log`。

合成运行期间发现本机已保存的总音量为 0，因此上述记录仅证明播放器事件与时序。结束测试后将总音量恢复为默认 75%，重新启动真实摄像头版本；欢迎语 `playing=True`、摄像头预览 `source=camera size=320x240`、本机歌曲 `loop=true` 均已确认，准备阶段音乐混音值恢复为非零。没有保存真人取景画面。这些验证不代表孩子真人动作准确率，也未测试电视输出延迟。

## 大招专属镜头与出手衔接（2026-09-09）

大招触发后增加 0.98 秒专属特写，使用现有背侧 45° 图集中第 4 号 L 形光线姿势，聚焦迪迦上半身和双手。镜头在 0.16 秒内拉近至 14° 视野角，保持后用 0.24 秒退回；峰值投影尺寸约为普通光线视野的 1.9 倍。怪兽淡出、普通 HUD 暂时隐藏，增加上下遮幅、边缘速度线、手部聚光及招式字幕。没有新增或下载角色图片。

`BeamCloseup` 活跃时仅将 Battle 的时间增量设为 0，每帧仍处理追踪与暂停输入。战吼在特写开始时播放，退镜后在光线首次可见时播放一次光线音效，随后按原有动作命中时刻扣血。暂停、重开、设置或角色展示立即清理特写；致胜不会在特写中途提前切到胜利姿势。

新增 15 项行为检查，覆盖 15/30/60 FPS 下特写冻结伤害/动作/怪兽计时、能量只消耗一次、退镜早于命中、光线单次伤害、稳定特写阶段、暂停与恢复、重开、展示中断及光线致胜。全量独立检查通过，共 **148 项**（12 项 Python、133 项 C#、3 项额外跨进程验证；跨进程输出 136 包含重复的 C# 检查）。证据：`logs/beam-closeup-checks.log`。

Unity 实际渲染检查通过：保留 16 个角色姿势、3 个战斗与 8 个怪兽攻击画面，新增 8 个特写/退镜/发射/致胜/暂停画面。断言峰值放大至少 1.8 倍、选择正确大招姿势、头手参考位置不出框、特写期间血量不变、退镜后只发射一次，以及暂停立即恢复普通视野。已查看特写、退镜和光线画面。证据：`logs/beam-closeup-render.log`、本机忽略目录 `artifacts/animation-review/beam-closeup-*.png`。

完整 macOS 构建成功。原生键盘试玩临时使用 20 次血量，15 次普攻蓄满后触发大招：依次记录 `key=beam_original`、`[BeamCloseup] begin`、`end phase=Battle action=Beam`、`beam-visible actionAge=0.28`、`cue=Victory`、新版胜利语音。窗口模式已查看带遮幅和字幕的特写；全屏再次查看特写并在中途按 Esc，镜头、普通 HUD 与暂停面板立即恢复，怪兽保持 5/20，取消的大招没有结算伤害。随后通过设置恢复并应用 50 次血量，确认准备画面为 50/50。证据：`logs/beam-closeup-play-proof.log`、`logs/beam-closeup-build-command.log`、`logs/beam-closeup-unity-build.log`。

最终已重新启动真实摄像头版本，日志确认相机预览 `source=camera size=320x240`、英雄欢迎配音以及本机歌曲循环载入；没有保存真人画面。上述键盘和合成渲染验证不代表孩子真人动作准确率或电视投屏延迟。

## 2026-09-09：胜利合照

新增胜利卡片相机按钮和 F7，5 秒倒计时后自动抠图合成；迪迦正面在左、镜像人物在右，共用城市夜景。1920×1080 PNG 保存到当前用户 Downloads，支持取消、重新拍摄和独立文件名。未取得新的人像时不保存。默认相机取景仍不录制，主动合照是单张图片保存入口。

- `scripts/check.sh`：14 项 Python、152 项 C#、3 项额外回环通信检查，共 **169 项独立检查**通过。新增检查覆盖五秒边界、重复点击、取消、重新倒计时、旧帧/未来帧、PNG 字节/尺寸/通道限制、重拍不覆盖及临时文件清理。集成输出中的 155 包含重复执行的 152 项 C#，不重复计数。
- 首次实机尝试 MediaPipe 的分割掩码时，Metal 路径报 `TensorsToSegmentationCalculator / upsample_program_` 错误。因此最终方案使用 macOS Vision 原生人像分割；MediaPipe 姿态配置保持原样。原生组件在构建时编译，无额外模型下载。
- `scripts/photo_smoke.py --camera`：本机摄像头连续收到 27 张 RGBA 帧，26 帧具有人像；首帧接收帧龄 612 ms，最后一帧 91 ms。检查完全透明区域的 RGB 已清除、掩码维度与画面一致、服务正常退出，**未保存真人图像**。这不等于精确头发边缘质量、儿童全身测试或电视端到端延迟。证据：`logs/photo-native-smoke.log`。
- Unity 原生构建成功。`PhotoReview.Render` 导出 1920×1080 合成测试图，人工查看了迪迦正面、左右分离、完整人物和城市背景；照片没有倒计时、提示、战斗 UI。证据：`artifacts/photo-review/photo-composition.png`、`logs/photo-render.log`。
- 使用 `--demo` 保留明显的 TEST 卡通人物，完成 50 血量实机战斗（41 次普攻 + 1 次 9 伤害光线），通过胜利合照入口测试。日志记录相机按钮 `mouseUp clicked=True`；原生画面已查看 5 秒倒计时、全屏等比构图、保存成功状态。取消后无保存事件；两次完成拍摄生成两个不同命名的 PNG，实际位于 `/Users/fankaibo/Downloads`，每张约 1.84 MB。照片不纳入 Git。证据：`logs/victory-photo-native-proof.log`。重拍文件不覆盖与重新倒计时还由纯逻辑检查覆盖。核验后已将两张合成测试照片移回忽略目录 `artifacts/photo-review`，避免 Downloads 留下测试文件。

## 2026-09-09：实时镂空取景与明确成片预览

根据用户反馈，将合照拆为“实时摆姿势 → 主动点击后倒计时 5 秒 → 成片预览”。原来打开就倒计时，拍摄前后标题相同；现在取景显示持续更新的透明人像和“这是你”站位角标，入镜后启用拍照。拍完标题改为“拍好了！看看我们的合照”，并标记“照片预览 · 已定格”，持续显示刚拍的照片直到重拍或完成。角标和提示不进入照片。

- 新增 10 项 `PhotoSession` 状态检查，覆盖打开不拍照、未入镜不倒计时、显式快门、取消回取景、拍摄瞬间断流后不补拍、成片持续停留、重拍先摆姿势、关闭取消等边界。完整门禁为 14 项 Python + 162 项 C# + 3 项额外通信，共 **179 项独立检查**。
- Unity 本机构建通过，使用会摆动双臂的 TEST 合成人物进行原生验收：进入取景超过 5 秒没有保存；不同截图中手臂动作变化；空格启动五秒倒计时，Esc 后回到仍在更新的取景，未产生文件。
- 再次主动拍摄生成 1920×1080 PNG，约 1.85 MB，保存到 Downloads；新标题、定格预览和保存提示均已查看。间隔超过 30 秒的两次预览截图字节完全相同，证明预览没有继续播放实时动作，也没有自动关闭。回车重拍回到实时画面，未立即开始倒计时。此次通过键盘快捷键完成流程验证；鼠标操作复用现有 HUD 按钮。
- 成片保存失败时保留 PNG 和预览，提供“重试保存”；取消倒计时后继续实时取景。相机、人像抠图算法和原有普通战斗配置保持原样。
- 证据：`logs/photo-live-preview-check.log`、`logs/photo-live-preview-build-command.log`、`logs/photo-live-preview-native-proof.log`。合成验证图片核验后移回忽略目录 `artifacts/photo-review`；未主动拍摄或保存真人图像。

## 2026-09-09：儿童实玩后的大招容差与提示反应时间

家长反馈孩子满意、普通动作识别较准确，大招较难触发，听懂并执行指令需要再留 2–3 秒。本次保留普攻与防御识别参数，仅放宽光线的 L 形、双手前推及短暂遮挡容差，并为普攻收尾时识别到的大招增加 0.8 秒缓冲。指令以实际语音开始播放为起点，保证语音结束后还有 3 秒反应时间；等待只限制怪兽，孩子可以立即出手。能量满后不会过期。

- `scripts/check.sh` 全部通过：14 项 Python、184 项 C#、3 项额外回环通信检查，共 **201 项独立检查**；跨进程输出的 187 包含重复的 C# 检查，不重复计数。新增 22 项覆盖较低的 L 形、较短的前推、部分遮挡、短暂丢腕、15/30/60 FPS、大招缓冲、延迟语音、反应窗口及暂停清理；原有单拳与护盾冲突回归仍通过。证据：`logs/child-play-checks.log`。
- Unity 6000.6.0f1 编译和 `scripts/build_macos.sh` 的 macOS Player 构建成功。最初沙盒构建不能访问 Unity 本机许可服务，清理本次挂起的许可进程并在获得审批的环境重试后完成；这不是源代码编译失败。证据：`logs/child-play-build-command.log`、`logs/unity-build.log` 的 `Build Finished, Result: Success.`。
- 新构建原生 `--demo` 全局试玩完成 **33 次普攻、2 次光线、1 次成功防御**，默认 50 血量下由第二次光线致胜，已查看胜利画面。日志确认战斗引导 2.25 秒对应 5.25 秒等待，能量提示 3.98 秒对应 6.98 秒等待，防御预警 2.24 秒对应至少 5.40 秒蓄力。该数值由实际播放器回调接入，反应期内的受击边界由上述规则检查验证。两次光线均依次播放 `beam_original`、进入和退出 0.98 秒专属镜头、再显示光线并结算伤害，最后正常播放胜利提示。证据：`logs/child-play-native-proof.log`。
- 两个 10 秒运行采样为 59.4 / 59.1 FPS，最慢帧为 22.3 / 33.4 ms；本机背景音乐正常载入循环播放。试玩使用明确标注的合成输入，没有打开真实摄像头、拍摄或保存真人图像；结束后关闭游戏及本次合成服务。新的大招容差仍需孩子后续实际试玩确认，未测量电视投屏延迟。
- 动画路线已按用户选择记录为“外形还原的精细 3D 模型与连续骨骼动作”，候选来源及尚未验证的项目见《3D角色升级方案》。当前构建仍使用原有动作插画，没有替换成 3D 模型；浏览器对 BlendSwap 的新增网站权限被自动审批拒绝，尚未取得候选模型文件。


## 迪迦骨骼模型首轮接入（2026-09-09）

用户授权访问 BlendSwap、完成登录后取得 Extrazhang 的迪迦模型。使用官方 Blender 4.5.13 LTS 在禁用内嵌脚本的条件下检查、提取纹理、重建材质和编排八段战斗动画，转换为 Unity 可直接加载的 FBX。实际导入 200 根骨骼、5 个渲染组件，按变形后顶点统一到 3.45 单位站高。修正 FBX 单位、蒙皮包围盒与动画缩放不一致、连续编排时已有关键帧覆盖新姿势，以及左右手指控制器不对称的问题。

现有完整检查仍为 201 项独立检查（14 Python、184 C#、3 额外跨进程），全部通过。首次沙箱测试因不能绑定本机端口报 EPERM，改在允许回环通信的环境重新运行通过，未修改规则绕过测试。日志：`logs/rigged-upgrade-checks.log`。

新增 Unity 实际素材检查验证身体高度和脚部基准，确认出拳、防御、光线的手腕位置不同且腰部确实变形。十秒、300 帧场景序列使用真实 Battle 规则：预先蓄能后两拳、一次格挡、一发光线、最终胜利；总计 17 次普攻、1 次格挡、0 次受击、1 次光线，尺寸/落地/骨骼位移与战斗断言通过。预览：`artifacts/rigged-combat/tiga-combat-preview.mp4`；日志：`logs/rigged-combat-review.log`。没有摄像头画面，预览不是影视录像。

macOS 开发版构建通过。已在原生游戏里查看左右拳、防御、光线张掌姿势；合成输入整局完成 **33 次普攻、1 次格挡、2 次光线并胜利**。两次大招均使用独立的 `beam_original` 0.89 秒战吼，本机歌曲持续载入。两个十秒性能窗口为 **59.3 / 59.4 FPS**，最慢帧 **20.4 / 37.2 ms**。已查看原生胜利画面，随后将胜利镜头放宽为 29°，留出举手顶部空间。证据：`logs/rigged-play-proof.log`。这些是本机合成验证，不表示新版本已经由孩子体验，也不表示电视投屏延迟已测量。

哥尔赞暂时保留动作图集；SFMLab 候选文件未取得，新增网站访问仍等待用户明确确认。城市仍为背景图，合照使用正面插画，整套动画演出尚未达到参考视频品质。本轮完成的是迪迦模型和第一套连续骨骼动作接入。素材作者、许可、修改和哈希见模型目录 `ATTRIBUTION.txt`；人物展示页和游戏包内同时保留署名。

## 双角色实战接入（2026-09-09，样片之后）

用户下载哥尔赞并查看样片后要求继续，已接入真实战斗状态与玩家构建。201 项现有检查通过；Unity 实战序列验证两名骨骼角色、17 次普攻/1 次格挡/1 次光线后胜利，另覆盖暂停、反击、手部特效和大招镜头。怪兽尾巴穿地由 -0.124 修正为最低约 -0.006 单位。后台玩家包连接合成姿态运行 18 秒，记录了变身、普攻和两名角色的加载；未打开真实摄像头。素材、参数、日志路径和未完成事项见 [双角色实战接入](双角色实战接入.md)。
